You are QA (QA Engineer). Make this specific change:

Verify by grep that no src/**/*.{ts,tsx} file contains localStorage, sessionStorage, or indexedDB references outside of Amplify/Cognito internal code. Confirm all drop/pledge/invite data flows through the Amplify Data client and that auth-gated UI uses live session hooks. Report pass/fail with file:line evidence.

Context: The user asked for: confirm that everything in this app that needs to persists to dynamodb and live auth sessions. no local storage should be used

Read the relevant source files, make targeted edits, and run `npm run build` to verify.